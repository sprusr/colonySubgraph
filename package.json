{
  "name": "colony-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ sprusr/colony",
    "create-local": "graph create --node http://localhost:8020/ sprusr/colony",
    "remove-local": "graph remove --node http://localhost:8020/ sprusr/colony",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sprusr/colony",
    "test": "graph codegen && graph build"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.14.0",
    "@graphprotocol/graph-ts": "0.13.0"
  }
}
